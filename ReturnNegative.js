function returnNegative(num) {
    return num > 0 ? num - (num + num) :  num
}

// we take number and check if its greater than 0, we return the value that is double negative of itself.
// else we return the number